import Form from "deco-sites/site-wave/components/ui/FormComponents.tsx";

interface Props {
  text: string;
}

const Contact = ({}: Props) => {
  return (
    <div class="bg-black">
    </div>
  );
};

export default Contact;
